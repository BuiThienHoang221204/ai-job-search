name: CI/CD Pipeline

on:
  push:
    branches: [main, dev]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: ai-job-search-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test-and-build:
    name: Validate framework and CLI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Lint skills and commands
        run: |
          python -m pip install --disable-pip-version-check pyyaml
          python tools/lint_skills.py
          python tools/check_framework_version.py

      - name: Run security guards
        run: python tools/security_guards.py

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm

      - name: Check workspace setup
        run: |
          pnpm install --frozen-lockfile
          pnpm run check

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Type-check and test every portal CLI
        run: |
          set -euo pipefail
          found=0
          for cli in .agents/skills/*/cli; do
            [ -f "$cli/package.json" ] || continue
            found=1
            echo "::group::$cli"
            (
              cd "$cli"
              bun install
              bun run typecheck
              if find tests -type f -name '*.test.ts' 2>/dev/null | grep -q .; then
                bun test
              else
                echo "Không có test fixture; bỏ qua."
              fi
            )
            echo "::endgroup::"
          done
          test "$found" -eq 1 || { echo "::error::Không tìm thấy portal CLI nào."; exit 1; }

      - name: Backend - build and lint
        working-directory: server
        run: |
          pnpm install --frozen-lockfile
          pnpm exec prisma generate
          pnpm run lint
          pnpm run build

  security-scan:
    name: Scan repository vulnerabilities
    runs-on: ubuntu-latest
    needs: test-and-build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          ignore-unfixed: true
          format: table
          severity: UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL
          exit-code: "0"

  docker-build:
    name: Build and push Docker image
    runs-on: ubuntu-latest
    needs: security-scan
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set image name
        run: |
          echo "IMAGE_NAME=${GITHUB_REPOSITORY,,}" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./server
          file: ./server/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ env.IMAGE_NAME }}/server:${{ github.ref_name }}
            ghcr.io/${{ env.IMAGE_NAME }}/server:${{ github.sha }}
            ghcr.io/${{ env.IMAGE_NAME }}/server:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-dev:
    name: Deploy to Development
    runs-on: ubuntu-latest
    needs: docker-build
    if: github.ref == 'refs/heads/dev'
    environment: development
    steps:
      - name: Validate deployment configuration
        env:
          DO_HOST: ${{ secrets.DO_HOST }}
          DO_USER: ${{ secrets.DO_USER }}
          DO_PASSWORD: ${{ secrets.DO_PASSWORD }}
          DO_DEPLOY_PATH: ${{ vars.DO_DEPLOY_PATH }}
        run: |
          test -n "$DO_HOST" || { echo "::error::Missing DO_HOST secret."; exit 1; }
          test -n "$DO_USER" || { echo "::error::Missing DO_USER secret."; exit 1; }
          test -n "$DO_PASSWORD" || { echo "::error::Missing DO_PASSWORD secret."; exit 1; }
          test -n "$DO_DEPLOY_PATH" || { echo "::error::Missing DO_DEPLOY_PATH variable."; exit 1; }

      - name: Set lowercase image name
        run: |
          echo "IMAGE_NAME=${GITHUB_REPOSITORY,,}" >> "$GITHUB_ENV"

      - name: Deploy to DigitalOcean
        uses: appleboy/ssh-action@v1.2.2
        env:
          DEPLOY_PATH: ${{ vars.DO_DEPLOY_PATH }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          IMAGE_TAG: dev
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USER }}
          password: ${{ secrets.DO_PASSWORD }}
          envs: DEPLOY_PATH,IMAGE_NAME,IMAGE_TAG
          script: |
            set -euo pipefail
            cd "$DEPLOY_PATH/server"
            if [ -f .env.development ]; then
              cp .env.development .env
            elif [ ! -f .env ]; then
              echo "Warning: .env.development not found. Copying .env.example as fallback..."
              cp .env.example .env
            fi
            export GITHUB_REPOSITORY="$IMAGE_NAME"
            export IMAGE_TAG="$IMAGE_TAG"
            docker compose pull app postgres
            docker compose up -d --remove-orphans
            docker compose exec app npx prisma migrate deploy --skip-generate

  deploy-prod:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: docker-build
    if: github.ref == 'refs/heads/main'
    environment: production
    steps:
      - name: Validate deployment configuration
        env:
          DO_HOST: ${{ secrets.DO_HOST }}
          DO_USER: ${{ secrets.DO_USER }}
          DO_PASSWORD: ${{ secrets.DO_PASSWORD }}
          DO_DEPLOY_PATH: ${{ vars.DO_DEPLOY_PATH }}
        run: |
          test -n "$DO_HOST" || { echo "::error::Missing DO_HOST secret."; exit 1; }
          test -n "$DO_USER" || { echo "::error::Missing DO_USER secret."; exit 1; }
          test -n "$DO_PASSWORD" || { echo "::error::Missing DO_PASSWORD secret."; exit 1; }
          test -n "$DO_DEPLOY_PATH" || { echo "::error::Missing DO_DEPLOY_PATH variable."; exit 1; }

      - name: Set lowercase image name
        run: |
          echo "IMAGE_NAME=${GITHUB_REPOSITORY,,}" >> "$GITHUB_ENV"

      - name: Deploy to DigitalOcean
        uses: appleboy/ssh-action@v1.2.2
        env:
          DEPLOY_PATH: ${{ vars.DO_DEPLOY_PATH }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          IMAGE_TAG: main
        with:
          host: ${{ secrets.DO_HOST }}
          username: ${{ secrets.DO_USER }}
          password: ${{ secrets.DO_PASSWORD }}
          envs: DEPLOY_PATH,IMAGE_NAME,IMAGE_TAG
          script: |
            set -euo pipefail
            cd "$DEPLOY_PATH/server"
            if [ -f .env.production ]; then
              cp .env.production .env
            elif [ ! -f .env ]; then
              echo "Warning: .env.production not found. Copying .env.example as fallback..."
              cp .env.example .env
            fi
            export GITHUB_REPOSITORY="$IMAGE_NAME"
            export IMAGE_TAG="$IMAGE_TAG"
            docker compose pull app postgres
            docker compose up -d --remove-orphans
            docker compose exec app npx prisma migrate deploy --skip-generate
